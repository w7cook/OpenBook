{
  "username"    : "${user.username}",
  "first_name"  : "${user.first_name}",
  "middle_name" : "${user.middle_name}",
  "last_name"   : "${user.last_name}",
  "posts"       : "/users/${user.id}/posts",
  "comments"    : "/users/${user.id}/comments",
  "events"      : "/users/${user.id}/events"
}