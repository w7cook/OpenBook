[
  #{list items:user.posts(), as:'item'} 
    {
      "title"   : "${item.title}",
      "author"  : "${item.author.username}",
      "type"    : "${item.postType}",
      "content" : "${item.content}"
    }#{if !item_isLast},#{/if}
  #{/list}
]